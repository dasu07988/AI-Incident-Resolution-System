# Set to "true" to use local mock JSON instead of the live backend
VITE_USE_MOCK_DATA=true

# Base URL of the deployed Enterprise AI REST API (n8n webhook root)
VITE_API_BASE_URL=https://your-n8n-instance.example.com/webhook/Enterprise AI
